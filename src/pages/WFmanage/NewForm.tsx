import React, { useState, useEffect, useMemo } from 'react';
import { Button, Card } from 'antd';
import { GlobalResParams } from '@/types/ITypes';

export default () => {
  return <Card title="表单新建">表单新建</Card>;
};
